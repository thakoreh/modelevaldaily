---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { MODEL_DATA_VERIFIED_ON, VERIFIED_MODELS } from '../data/verified-models';

// Use verified models data directly (more reliable than parsing markdown)
const models = VERIFIED_MODELS.slice(0, 5).map(model => ({
	name: model.name,
	total: (model.scores.coding * 0.4 + model.scores.reasoning * 0.35 + model.scores.toolUse * 0.25),
	color: model.color
}));

const decisionCards = [
	{
		title: 'Build Coding Agents',
		href: '/coding-agents',
		description: 'Model routing for planning, coding, review, repo Q&A, bulk edits, and private codebases.',
	},
	{
		title: 'Pick a Model',
		href: '/tools/model-picker',
		description: 'Answer workflow, budget, context, and deployment questions to get a practical shortlist.',
	},
	{
		title: 'Compare APIs',
		href: '/compare',
		description: 'Compare model scores, context windows, provider tradeoffs, and pricing side by side.',
	},
];

const architectureCards = [
	{ title: 'Plan', model: 'GPT-5.5 / Claude Opus 4.7', description: 'Decompose work, identify files, set constraints, and decide when to escalate.' },
	{ title: 'Edit', model: 'GPT-5.4 / GPT-5.2-Codex', description: 'Generate diffs, run tests, fix failures, and keep changes scoped.' },
	{ title: 'Review', model: 'Claude Opus 4.7 / Sonnet 4.6', description: 'Audit regressions, edge cases, architecture risk, and unclear assumptions.' },
];
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`${SITE_TITLE} — Coding Agent Model Guide`} description={SITE_DESCRIPTION} />
		<style>
			/* Override main for homepage — full-width sections */
			main {
				width: 100%;
				max-width: 100%;
				padding: 0;
			}

			/* Shared container */
			.container {
				max-width: var(--container-xl);
				margin: 0 auto;
				padding: 0 1.5rem;
			}

			/* ===== HERO ===== */
			.hero-section {
				position: relative;
				padding: 4.6rem 0 3.5rem;
				overflow: hidden;
				border-bottom: 1px solid var(--border-secondary);
			}
			.hero-bg {
				position: absolute;
				inset: 0;
				background: var(--gradient-hero);
				pointer-events: none;
			}
			.hero-mesh {
				position: absolute;
				inset: 0;
				background: var(--gradient-mesh);
				opacity: 0.42;
				pointer-events: none;
			}
			.hero-grid-lines {
				position: absolute;
				inset: 0;
				background-image:
					linear-gradient(rgba(244,241,232,0.022) 1px, transparent 1px),
					linear-gradient(90deg, rgba(244,241,232,0.022) 1px, transparent 1px);
				background-size: 72px 72px;
				pointer-events: none;
			}
			.hero-content {
				position: relative;
				display: grid;
				grid-template-columns: minmax(0, 1fr) minmax(360px, 0.92fr);
				gap: 4rem;
				align-items: center;
			}
			.hero-text {
				opacity: 0;
				animation: fadeInUp 0.7s ease-out 0.1s forwards;
			}
			.hero-eyebrow {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				font-size: var(--font-xs);
				font-weight: 700;
				letter-spacing: 0;
				color: var(--accent-light);
				margin-bottom: var(--space-md);
				padding: 0.35rem 0.85rem;
				background: rgba(20, 184, 166, 0.1);
				border: 1px solid var(--border-accent);
				border-radius: var(--radius-md);
			}
			.hero-eyebrow .dot {
				width: 6px;
				height: 6px;
				border-radius: 50%;
				background: var(--success);
				animation: pulse 2s ease-in-out infinite;
			}
			.hero-title {
				font-size: clamp(2.65rem, 4.7vw, 4.35rem);
				line-height: 1.02;
				margin-bottom: var(--space-lg);
				letter-spacing: 0;
				color: var(--text-primary);
				background: none;
				-webkit-text-fill-color: currentColor;
			}
			.hero-desc {
				font-size: 1.05rem;
				color: var(--text-secondary);
				line-height: 1.65;
				max-width: 620px;
				margin-bottom: var(--space-xl);
			}
			.hero-badges {
				display: flex;
				gap: 0.5rem;
				flex-wrap: wrap;
				margin-bottom: var(--space-xl);
			}
			.hero-badges .badge {
				white-space: nowrap;
			}
			.hero-actions {
				display: flex;
				gap: 0.75rem;
				flex-wrap: wrap;
			}

			/* Buttons */
			.btn {
				display: inline-flex;
				align-items: center;
				justify-content: center;
				gap: 0.5rem;
				min-height: 44px;
				padding: 0.72rem 1.1rem;
				border-radius: var(--radius-md);
				font-weight: 700;
				font-size: var(--font-sm);
				text-decoration: none;
				transition: all var(--transition-fast);
				cursor: pointer;
				border: none;
			}
			.btn-primary {
				background: var(--accent);
				color: var(--text-inverse);
				box-shadow: none;
			}
			.btn-primary:hover {
				transform: translateY(-2px);
				box-shadow: 0 14px 30px rgba(20, 184, 166, 0.16);
				color: var(--text-inverse);
			}
			.btn-secondary {
				background: rgba(244, 241, 232, 0.05);
				color: var(--text-primary);
				border: 1px solid var(--border-primary);
			}
			.btn-secondary:hover {
				background: var(--bg-elevated);
				border-color: var(--border-accent);
				color: var(--text-primary);
			}
			.btn-arrow {
				transition: transform var(--transition-fast);
			}
			.btn:hover .btn-arrow {
				transform: translateX(3px);
			}

			/* ===== LEADERBOARD CARD ===== */
			.hero-visual {
				opacity: 0;
				animation: fadeInUp 0.7s ease-out 0.3s forwards;
			}
			.leaderboard-card {
				background: linear-gradient(180deg, rgba(25, 28, 23, 0.96), rgba(15, 17, 14, 0.96));
				border: 1px solid var(--border-primary);
				border-radius: var(--radius-xl);
				padding: 1.35rem;
				box-shadow: var(--shadow-lg);
				position: relative;
				overflow: hidden;
			}
			.leaderboard-link {
				position: absolute;
				inset: 0;
				z-index: 1;
			}
			.model-row {
				display: grid;
				grid-template-columns: auto 1fr auto;
				gap: var(--space-md);
				align-items: center;
				padding: 0.75rem 0;
				border-bottom: 1px solid var(--border-secondary);
				min-width: 0;
			}
			.model-row:last-of-type {
				border-bottom: none;
			}
			.model-name {
				font-weight: 600;
				font-size: var(--font-sm);
				color: var(--text-primary);
				white-space: nowrap;
				overflow: hidden;
				text-overflow: ellipsis;
			}
			.leaderboard-card::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				height: 3px;
				background: linear-gradient(90deg, var(--accent), var(--accent-2), var(--accent-3));
				opacity: 0.88;
			}
			.leaderboard-header {
				display: flex;
				justify-content: space-between;
				align-items: center;
				margin-bottom: var(--space-md);
			}
			.leaderboard-header h3 {
				font-size: var(--font-base);
				color: var(--text-primary);
				margin: 0;
			}
			.leaderboard-header span {
				font-size: var(--font-xs);
				color: var(--text-tertiary);
			}
			.model-rank {
				width: 28px;
				height: 28px;
				display: flex;
				align-items: center;
				justify-content: center;
				border-radius: var(--radius-md);
				font-size: var(--font-xs);
				font-weight: 700;
				background: rgba(244, 241, 232, 0.06);
				color: var(--text-secondary);
			}
			.model-rank.gold {
				background: rgba(245, 158, 11, 0.16);
				color: var(--warning);
			}
			.model-info {
				display: flex;
				flex-direction: column;
				gap: 0.15rem;
			}
			.model-name {
				font-weight: 600;
				font-size: var(--font-sm);
				color: var(--text-primary);
			}
			.model-bar-container {
				height: 6px;
				background: rgba(244, 241, 232, 0.08);
				border-radius: 3px;
				overflow: hidden;
				margin-top: 0.2rem;
			}
			.model-bar {
				height: 100%;
				border-radius: 3px;
				transition: width 1s ease-out;
			}
			.model-score {
				font-size: var(--font-lg);
				font-weight: 700;
				color: var(--text-primary);
			}
			.leaderboard-footer {
				margin-top: var(--space-md);
				padding-top: var(--space-sm);
				border-top: 1px solid var(--border-secondary);
				display: flex;
				justify-content: space-between;
				align-items: center;
			}
			.leaderboard-footer a {
				font-size: var(--font-xs);
				color: var(--accent-light);
			}
			.leaderboard-footer span {
				font-size: var(--font-xs);
				color: var(--text-tertiary);
			}

			/* ===== STATS STRIP ===== */
			.stats-strip {
				display: grid;
				grid-template-columns: repeat(4, 1fr);
				gap: 1px;
				padding: 0;
				border: 1px solid var(--border-primary);
				border-radius: var(--radius-lg);
				overflow: hidden;
				background: var(--border-primary);
			}
			.stat-card {
				background: rgba(18, 20, 16, 0.95);
				border: 0;
				border-radius: 0;
				padding: 1.25rem;
				text-align: left;
				transition: all var(--transition-base);
			}
			.stat-card:hover {
				background: var(--bg-card-hover);
				transform: none;
			}
			.stat-number {
				font-size: var(--font-2xl);
				font-weight: 700;
				color: var(--text-primary);
				line-height: 1;
				margin-bottom: 0.35rem;
			}
			.stat-label {
				font-size: var(--font-sm);
				color: var(--text-tertiary);
			}

			/* ===== SECTIONS ===== */
			.section {
				padding: 3.25rem 0;
			}
			.section-header {
				margin-bottom: var(--space-xl);
				display: flex;
				align-items: end;
				justify-content: space-between;
				gap: var(--space-xl);
			}
			.section-header h2 {
				margin-bottom: 0.35rem;
			}
			.section-header p {
				color: var(--text-tertiary);
				font-size: var(--font-base);
				max-width: 560px;
				margin: 0;
			}

			/* ===== POST GRID ===== */
			.post-grid {
				display: grid;
				grid-template-columns: repeat(3, 1fr);
				gap: 1rem;
			}
			.post-card {
				background: rgba(18, 20, 16, 0.88);
				border: 1px solid var(--border-primary);
				border-radius: var(--radius-lg);
				overflow: hidden;
				text-decoration: none;
				color: inherit;
				transition: all var(--transition-base);
				display: flex;
				flex-direction: column;
			}
			.post-card:hover {
				border-color: var(--border-accent);
				transform: translateY(-3px);
				box-shadow: var(--shadow-lg);
			}
			.post-card:hover .post-card-title {
				color: var(--accent-light);
			}
			.post-card-image {
				aspect-ratio: 16/9;
				overflow: hidden;
			}
			.post-card-image img {
				width: 100%;
				height: 100%;
				object-fit: cover;
				border-radius: 0;
				transition: transform var(--transition-slow);
			}
			.post-card:hover .post-card-image img {
				transform: scale(1.05);
			}
			.post-card-body {
				padding: var(--space-lg);
				flex: 1;
				display: flex;
				flex-direction: column;
			}
			.post-card-date {
				font-size: var(--font-xs);
				color: var(--text-tertiary);
				margin-bottom: 0.35rem;
			}
			.post-card-title {
				font-size: var(--font-base);
				font-weight: 600;
				color: var(--text-primary);
				line-height: 1.35;
				margin: 0;
				transition: color var(--transition-fast);
			}

			/* ===== TRUST GRID ===== */
			.trust-grid {
				display: grid;
				grid-template-columns: repeat(3, 1fr);
				gap: 1rem;
			}
			/* How It Works - 4 columns on desktop */
			.trust-grid.how-it-works {
				grid-template-columns: repeat(4, 1fr);
			}
			.trust-card {
				background: rgba(18, 20, 16, 0.88);
				border: 1px solid var(--border-primary);
				border-radius: var(--radius-lg);
				padding: 1.35rem;
				transition: all var(--transition-base);
				position: relative;
				overflow: hidden;
			}
			.trust-card::before {
				content: none;
			}
			.trust-card:hover {
				border-color: var(--border-accent);
				transform: translateY(-3px);
			}
			.trust-icon {
				width: 40px;
				height: 40px;
				border-radius: var(--radius-md);
				background: rgba(20, 184, 166, 0.1);
				border: 1px solid var(--border-primary);
				display: flex;
				align-items: center;
				justify-content: center;
				margin-bottom: var(--space-md);
				font-size: 1.2rem;
				position: relative;
			}
			.trust-card h3 {
				font-size: var(--font-base);
				margin-bottom: 0.5rem;
			}
			.trust-card p {
				font-size: var(--font-sm);
				color: var(--text-tertiary);
				margin: 0;
				line-height: 1.6;
			}

			/* ===== FIELDS TABLE ===== */
			.fields-section {
				background: linear-gradient(180deg, rgba(18, 20, 16, 0.94), rgba(16, 17, 15, 0.94));
				border: 1px solid var(--border-primary);
				border-radius: var(--radius-xl);
				padding: var(--space-2xl);
				overflow-x: auto;
			}
			.fields-section h2 {
				margin-bottom: var(--space-lg);
			}

			/* ===== NEW PAGES SECTION ===== */
			.provider-grid {
				display: grid;
				grid-template-columns: repeat(3, 1fr);
				gap: 1rem;
			}
			.provider-card {
				background: rgba(18, 20, 16, 0.9);
				border: 1px solid var(--border-primary);
				border-radius: var(--radius-lg);
				padding: 1.35rem;
				text-decoration: none;
				color: inherit;
				transition: all var(--transition-base);
			}
			.provider-card:hover {
				border-color: var(--border-accent);
				transform: translateY(-3px);
			}
			.provider-card h3 {
				font-size: var(--font-lg);
				margin-bottom: var(--space-sm);
			}
			.provider-card p {
				font-size: var(--font-sm);
				color: var(--text-secondary);
			}
			.new-pages-grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: var(--space-lg);
			}
			.new-page-card {
				background: linear-gradient(135deg, rgba(20, 184, 166, 0.14), rgba(245, 158, 11, 0.08)), var(--bg-card);
				border: 1px solid var(--border-primary);
				border-radius: var(--radius-lg);
				padding: var(--space-xl);
				text-decoration: none;
				color: inherit;
				transition: all var(--transition-base);
			}
			.new-page-card:hover {
				border-color: var(--accent);
				transform: translateY(-4px);
			}
			.new-page-icon {
				font-size: 2rem;
				margin-bottom: var(--space-sm);
			}
			.new-page-card h3 {
				font-size: var(--font-lg);
				margin-bottom: var(--space-sm);
			}
			.new-page-card p {
				font-size: var(--font-sm);
				color: var(--text-secondary);
				margin-bottom: var(--space-md);
			}
			.new-page-link {
				font-size: var(--font-sm);
				font-weight: 600;
				color: var(--accent-light);
			}

			/* ===== CTA ===== */
			.cta-section {
				padding: var(--space-3xl) 0 var(--space-xl);
			}
			.cta-card {
				background: var(--bg-card);
				border: 1px solid var(--border-accent);
				border-radius: var(--radius-xl);
				padding: 2.5rem;
				text-align: center;
				position: relative;
				overflow: hidden;
			}
			.cta-card::before {
				content: '';
				position: absolute;
				inset: 0;
				background: var(--gradient-card);
				pointer-events: none;
			}
			.cta-card h2 {
				position: relative;
				margin-bottom: var(--space-sm);
			}
			.cta-card p {
				position: relative;
				color: var(--text-secondary);
				margin-bottom: var(--space-xl);
				max-width: 480px;
				margin-left: auto;
				margin-right: auto;
			}
			.cta-card .btn {
				position: relative;
			}

			/* ===== RESPONSIVE ===== */
			@media (max-width: 960px) {
				.hero-content {
					grid-template-columns: 1fr;
					gap: var(--space-2xl);
				}
				.hero-title {
					font-size: clamp(1.75rem, 5vw, 2.5rem);
				}
				.hero-desc {
					font-size: var(--font-base);
				}
				.post-grid {
					grid-template-columns: repeat(2, 1fr);
					gap: var(--space-md);
				}
				.trust-grid {
					grid-template-columns: 1fr;
				}
				/* How It Works - 2 columns on tablet */
				.trust-grid.how-it-works {
					grid-template-columns: repeat(2, 1fr);
				}
				.stats-strip {
					grid-template-columns: repeat(2, 1fr);
				}
				.leaderboard-footer {
					flex-direction: column;
					gap: var(--space-xs);
					text-align: center;
				}
			}
			@media (max-width: 600px) {
				.hero-section {
					padding: var(--space-xl) 0;
				}
				.hero-title {
					font-size: 2.15rem;
					line-height: 1.05;
				}
				.hero-badges {
					flex-wrap: wrap;
				}
				.hero-actions {
					flex-direction: column;
				}
				.hero-actions .btn {
					width: 100%;
					justify-content: center;
				}
				.post-grid {
					grid-template-columns: 1fr;
				}
				.stats-strip {
					grid-template-columns: 1fr 1fr;
					gap: var(--space-sm);
				}
				.stat-card {
					padding: var(--space-md);
				}
				.stat-number {
					font-size: var(--font-2xl);
				}
				.section {
					padding: var(--space-xl) 0;
				}
				.section-header {
					margin-bottom: var(--space-md);
					display: block;
				}
				.trust-card {
					padding: var(--space-lg);
				}
				/* How It Works - 1 column on mobile */
				.trust-grid.how-it-works {
					grid-template-columns: 1fr;
				}
				.fields-section {
					padding: var(--space-lg);
				}
				.fields-section table th,
				.fields-section table td {
					white-space: normal;
					min-width: 0;
				}
				.provider-grid {
					grid-template-columns: 1fr;
				}
				.cta-card {
					padding: var(--space-xl);
				}
				.cta-card h2 {
					font-size: var(--font-xl);
				}
				
				/* Mobile leaderboard - card view */
				.leaderboard-card {
					padding: var(--space-md);
				}
				.leaderboard-header h3 {
					font-size: var(--font-base);
				}
				.model-row {
					padding: var(--space-sm) 0;
					gap: var(--space-sm);
				}
				.model-name {
					font-size: var(--font-xs);
					max-width: 140px;
				}
				.model-score {
					font-size: var(--font-base);
				}
				.model-bar-container {
					display: none;
				}
				.leaderboard-footer {
					flex-direction: column;
					gap: var(--space-xs);
					text-align: center;
				}
				.leaderboard-footer span {
					font-size: 10px;
				}
			}
			
			/* Extra small devices (phones, 400px and below) */
			@media (max-width: 400px) {
				.hero-title {
					font-size: 2rem;
					line-height: 1.06;
				}
				.hero-desc {
					font-size: var(--font-sm);
				}
				.badge {
					font-size: 10px;
					padding: 0.25rem 0.5rem;
				}
				.leaderboard-card {
					border-radius: var(--radius-lg);
				}
				.model-rank {
					width: 24px;
					height: 24px;
					font-size: 10px;
				}
				.model-name {
					font-size: 11px;
					max-width: 110px;
				}
			}
		</style>
	</head>
	<body>
		<Header />

		<!-- Hero -->
		<section class="hero-section">
			<div class="hero-bg"></div>
			<div class="hero-mesh"></div>
			<div class="hero-grid-lines"></div>
			<div class="hero-content container">
				<div class="hero-text">
					<span class="hero-eyebrow">
						<span class="dot"></span>
						Model data verified {MODEL_DATA_VERIFIED_ON}
					</span>
					<h1 class="hero-title">Build better coding agents with the right AI models.</h1>
					<p class="hero-desc">
						Choose models for coding, agent workflows, long-context analysis, and production API routing. We focus on what developers actually need: <strong>code quality</strong>, <strong>tool use</strong>, <strong>context</strong>, <strong>price</strong>, and <strong>reliability</strong>.
					</p>
					<div class="hero-badges">
						<span class="badge badge-accent">Coding-agent focused</span>
						<span class="badge">Model routing guidance</span>
						<span class="badge">API price checks</span>
						<span class="badge badge-success">Source-reviewed data</span>
					</div>
					<div class="hero-actions">
						<a class="btn btn-primary" href="/coding-agents">
							Build coding agents
							<span class="btn-arrow">&rarr;</span>
						</a>
						<a class="btn btn-secondary" href="/tools/model-picker">Pick a model</a>
					</div>
				</div>

				<div class="hero-visual">
					<div class="leaderboard-card">
						<a href="/models" class="leaderboard-link"></a>
						<div class="leaderboard-header">
							<h3>Verified Leaderboard</h3>
							<span>{MODEL_DATA_VERIFIED_ON}</span>
						</div>
						{models.map((model, i) => (
							<div class="model-row">
								<div class:list={['model-rank', { gold: i === 0 }]}>
									{i + 1}
								</div>
								<div class="model-info">
									<span class="model-name">{model.name}</span>
									<div class="model-bar-container">
										<div
											class="model-bar"
											style={`width: ${(model.total / 10) * 100}%; background: ${model.color};`}
										></div>
									</div>
								</div>
								<div class="model-score">{model.total.toFixed(1)}</div>
							</div>
						))}
						<div class="leaderboard-footer">
							<a href="/models">View all models &rarr;</a>
							<span>Coding 40% / Reasoning 35% / Tool-use 25%</span>
						</div>
					</div>
				</div>
			</div>
		</section>

		<main>
			<!-- Stats strip -->
			<section class="section">
				<div class="container">
					<div class="stats-strip">
						<div class="stat-card">
							<div class="stat-number">25</div>
							<div class="stat-label">Models in source log</div>
						</div>
						<div class="stat-card">
							<div class="stat-number">10+</div>
							<div class="stat-label">AI providers</div>
						</div>
						<div class="stat-card">
							<div class="stat-number">4</div>
							<div class="stat-label">Core routing roles</div>
						</div>
						<div class="stat-card">
							<div class="stat-number">Verified</div>
							<div class="stat-label">Updated after source review</div>
						</div>
					</div>
				</div>
			</section>

			<!-- High-value paths -->
			<section class="section">
				<div class="container">
					<div class="section-header">
						<h2>Start With the Job You Need Done</h2>
						<p>Skip archive browsing. These are the practical paths for developers choosing models for real systems.</p>
					</div>
					<div class="post-grid">
						{decisionCards.map((card) => (
							<a class="post-card text-card" href={card.href}>
								<div class="post-card-body">
									<span class="post-card-date">Decision tool</span>
									<h3 class="post-card-title">{card.title}</h3>
									<p class="post-card-excerpt">{card.description}</p>
								</div>
							</a>
						))}
					</div>
				</div>
			</section>

			<!-- Why engineers trust this -->
			<section class="section">
				<div class="container">
					<div class="section-header">
						<h2>Built for Coding and Agent Workflows</h2>
						<p>Most model sites stop at rankings. This one is structured around production decisions.</p>
					</div>
					<div class="trust-grid">
						<div class="trust-card">
							<div class="trust-icon">&#9881;</div>
							<h3>Agent routing guidance</h3>
							<p>Separate planning, coding, reviewing, and bulk work instead of forcing one model to do everything.</p>
						</div>
						<div class="trust-card">
							<div class="trust-icon">&#128270;</div>
							<h3>Developer-first criteria</h3>
							<p>Score coding quality, tool use, context, and price together because agent systems need all four.</p>
						</div>
						<div class="trust-card">
							<div class="trust-icon">&#9889;</div>
							<h3>Source-reviewed facts</h3>
							<p>Model availability, context, and pricing are tied back to source data so claims can be audited.</p>
						</div>
					</div>
				</div>
			</section>

			<!-- Agent architecture -->
			<section class="section">
				<div class="container">
					<div class="section-header">
						<h2>A Better AI Agent Stack</h2>
						<p>Use the right model for each stage instead of spending frontier-model money on every token.</p>
					</div>
					<div class="trust-grid how-it-works">
						{architectureCards.map((card, index) => (
							<div class="trust-card">
								<div class="trust-icon">{index + 1}</div>
								<h3>{card.title}</h3>
								<p><strong>{card.model}</strong></p>
								<p>{card.description}</p>
							</div>
						))}
					</div>
				</div>
			</section>

			<!-- What we track -->
			<section class="section">
				<div class="container">
					<div class="fields-section">
						<h2>What We Track</h2>
						<table>
							<thead>
								<tr>
									<th>Category</th>
									<th>Fields</th>
								</tr>
							</thead>
							<tbody>
								<tr>
									<td><strong>Performance</strong></td>
									<td>Coding score, tool-use score, reasoning score, context window</td>
								</tr>
								<tr>
									<td><strong>Cost</strong></td>
									<td>Input/output price per 1M tokens, context pricing notes, provider tradeoffs</td>
								</tr>
								<tr>
									<td><strong>Latency</strong></td>
									<td>Best-fit workflow, routing role, escalation path, production caveats</td>
								</tr>
								<tr>
									<td><strong>Reliability</strong></td>
									<td>Source links, verification date, stale-claim audit flags</td>
								</tr>
							</tbody>
						</table>
					</div>
				</div>
			</section>

			<!-- Provider pages -->
			<section class="section" style="background: var(--bg-secondary);">
				<div class="container">
					<div class="section-header">
						<h2>Provider and Deployment Guides</h2>
						<p>Go deeper when you already know your provider, budget, or deployment constraint.</p>
					</div>
					<div class="provider-grid">
						<a href="/best-openai-models" class="provider-card"><h3>Best OpenAI Models</h3><p>GPT-5.5, GPT-5.4, GPT-5.2-Codex, and more</p></a>
						<a href="/best-anthropic-models" class="provider-card"><h3>Best Anthropic Models</h3><p>Claude Opus 4.7, Sonnet 4.6</p></a>
						<a href="/best-google-models" class="provider-card"><h3>Best Google Models</h3><p>Gemini 3.1 Pro Preview, Gemini 3 Flash, and more</p></a>
						<a href="/local-models" class="provider-card"><h3>Local Model Guide</h3><p>Private deployment, open weights, and self-hosting tradeoffs</p></a>
						<a href="/cheapest-ai-models" class="provider-card"><h3>Cheapest AI Models</h3><p>Lower-cost API options for high-volume workloads</p></a>
						<a href="/model-data" class="provider-card"><h3>Source Data</h3><p>Verification dates, source links, pricing, and context windows</p></a>
					</div>
				</div>
			</section>

			<!-- CTA -->
			<section class="cta-section">
				<div class="container">
					<div class="cta-card">
						<h2>Build a smarter model router</h2>
						<p>Start with coding-agent routing, then use the model picker to tune for cost, context, and deployment constraints.</p>
						<a class="btn btn-primary" href="/coding-agents">
							Open coding-agent guide
							<span class="btn-arrow">&rarr;</span>
						</a>
					</div>
				</div>
			</section>
		</main>

		<Footer />
	</body>
</html>
