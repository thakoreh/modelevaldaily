---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.hero {
				display: grid;
				gap: 1.5rem;
				grid-template-columns: 1.2fr 0.8fr;
				align-items: center;
				margin-bottom: 2rem;
			}
			.hero-card {
				background: var(--surface);
				border-radius: 16px;
				padding: 1.5rem;
				box-shadow: var(--box-shadow);
				border: 1px solid rgba(var(--border), 0.7);
			}
			.hero h1 {
				font-size: 2.6rem;
				margin-bottom: 0.5rem;
			}
			.hero p {
				margin-bottom: 1rem;
			}
			.meta {
				color: rgb(var(--text-muted));
				font-size: 0.95rem;
			}
			.badges {
				display: flex;
				gap: 0.75rem;
				flex-wrap: wrap;
			}
			.badge {
				font-size: 0.85rem;
				padding: 0.35rem 0.75rem;
				border-radius: 999px;
				background: rgba(var(--border), 0.6);
				color: rgb(var(--text));
			}
			.cta {
				display: inline-flex;
				align-items: center;
				gap: 0.5rem;
				padding: 0.6rem 1.1rem;
				border-radius: 10px;
				background: var(--accent);
				color: white;
				text-decoration: none;
				font-weight: 700;
			}
			.cta.secondary {
				background: transparent;
				color: var(--accent);
				border: 2px solid var(--accent);
			}
			.grid {
				display: grid;
				grid-template-columns: repeat(3, 1fr);
				gap: 1.25rem;
			}
			.card {
				background: var(--surface);
				border-radius: 14px;
				padding: 1.25rem;
				border: 1px solid rgba(var(--border), 0.7);
			}
			.card h3 {
				margin-bottom: 0.5rem;
			}
			@media (max-width: 960px) {
				.hero {
					grid-template-columns: 1fr;
				}
				.grid {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<div>
					<h1>Know which model wins today.</h1>
					<p>
						ModelEval Daily publishes impartial scorecards across real-world tasks—performance,
						cost, and latency in one place.
					</p>
					<p class="meta">Updated daily at 6am ET · <strong>4 models</strong> tested</p>
					<div class="badges">
						<span class="badge">Daily scorecards</span>
						<span class="badge">Transparent methodology</span>
						<span class="badge">Cost + latency tracked</span>
					</div>
					<div style="margin-top: 1.25rem; display: flex; gap: 0.75rem; flex-wrap: wrap;">
						<a class="cta" href="/blog">View today’s scorecard</a>
						<a class="cta secondary" href="/about">Methodology</a>
					</div>
				</div>
				<div class="hero-card">
					<h3>Today’s highlights</h3>
					<ul>
						<li>Overall winner + task leaders (code, reasoning, retrieval)</li>
						<li>Latency + cost leaderboard</li>
						<li>Failure case + fix</li>
					</ul>
					<hr />
					<h3>Why it matters</h3>
					<p>
						Most benchmarks ignore workflows. We test models like real engineers do.
					</p>
				</div>
			</section>

			<section class="grid">
				<div class="card">
					<h3>Daily rankings</h3>
					<p>Scan results in 60 seconds and ship the right model this week.</p>
				</div>
				<div class="card">
					<h3>Open scorecards</h3>
					<p>Prompts and grading rubrics are public and reproducible.</p>
				</div>
				<div class="card">
					<h3>Cost & latency</h3>
					<p>We track speed and spend so benchmarks reflect reality.</p>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
