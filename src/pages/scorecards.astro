---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const latest = posts[0];
const highlights = posts.slice(1, 5);
const taxonomy = [
	{ title: 'Coding', description: 'Bug fixes, refactors, API patches, diffs.' },
	{ title: 'Reasoning', description: 'Decisions, tradeoffs, leadership calls.' },
	{ title: 'Tool‑use', description: 'Docs, CLI flows, integrations.' },
	{ title: 'RAG/Research', description: 'Retrieval, synthesis, audit trails.' },
	{ title: 'Agents', description: 'Multi‑step plans, tool reliability.' },
	{ title: 'Latency/Cost', description: 'Speed, spend, and throughput tradeoffs.' },
];
const filters = [
	{ label: 'All', href: '#recent' },
	{ label: 'Coding', href: '#coding' },
	{ label: 'Reasoning', href: '#reasoning' },
	{ label: 'Tool‑use', href: '#tool-use' },
	{ label: 'RAG/Research', href: '#rag' },
	{ label: 'Agents', href: '#agents' },
];
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`${SITE_TITLE} | Scorecards`} description={SITE_DESCRIPTION} />
		<style>
			main {
				width: 1040px;
			}
			.hero {
				display: grid;
				grid-template-columns: 1.2fr 0.8fr;
				gap: 1.5rem;
				margin-bottom: 2rem;
			}
			.hero-card {
				background: var(--surface);
				border-radius: 16px;
				padding: 1.5rem;
				box-shadow: var(--box-shadow);
				border: 1px solid rgba(var(--border), 0.7);
			}
			.hero p {
				color: rgb(var(--text-muted));
			}
			.filters {
				display: flex;
				gap: 0.5rem;
				flex-wrap: wrap;
				margin: 1rem 0 2rem;
			}
			.filter {
				border: 1px solid rgba(var(--border), 0.6);
				border-radius: 999px;
				padding: 0.35rem 0.9rem;
				background: rgba(var(--border), 0.3);
				font-size: 0.9rem;
				text-decoration: none;
				color: rgb(var(--text));
			}
			.filter:hover {
				border-color: rgba(var(--accent), 0.6);
				color: var(--accent);
			}
			.grid {
				display: grid;
				grid-template-columns: repeat(2, 1fr);
				gap: 1.5rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			.card {
				background: var(--surface);
				border: 1px solid rgba(var(--border), 0.7);
				padding: 1rem;
				border-radius: 12px;
			}
			.title {
				margin: 0 0 0.25rem 0;
				color: rgb(var(--text));
				line-height: 1.2;
				font-size: 1.25rem;
			}
			.date {
				margin: 0;
				color: rgb(var(--text-muted));
			}
			.card img {
				border-radius: 12px;
				margin-bottom: 0.75rem;
			}
			.card a {
				display: block;
				text-decoration: none;
			}
			.card a:hover h4,
			.card a:hover .date {
				color: var(--accent);
			}
			.taxonomy {
				display: grid;
				grid-template-columns: repeat(3, 1fr);
				gap: 1rem;
				margin: 2rem 0;
			}
			.taxonomy-card {
				background: rgba(var(--border), 0.2);
				border: 1px solid rgba(var(--border), 0.5);
				border-radius: 12px;
				padding: 1rem;
			}
			.section {
				margin: 2.5rem 0;
			}
			@media (max-width: 960px) {
				.hero {
					grid-template-columns: 1fr;
				}
				.grid {
					grid-template-columns: 1fr;
				}
				.taxonomy {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<div>
					<h1>Daily AI model scorecards</h1>
					<p>Browse today’s winner, task leaders, and latency/cost tradeoffs.</p>
					<p><strong>Updated daily at 6am ET.</strong></p>
					<div class="filters">
						{filters.map((filter) => (
							<a class="filter" href={filter.href}>{filter.label}</a>
						))}
					</div>
					<p class="meta">Filters link to scorecard types. Interactive filtering is coming.</p>
				</div>
				{latest && (
					<div class="hero-card">
						<h3>Today’s scorecard</h3>
						<a href={`/blog/${latest.id}/`}>
							{latest.data.heroImage && (
								<Image width={720} height={360} src={latest.data.heroImage} alt="" />
							)}
							<h4 class="title">{latest.data.title}</h4>
							<p class="date">
								<FormattedDate date={latest.data.pubDate} />
							</p>
						</a>
					</div>
				)}
			</section>

			<section class="section">
				<h2>Scorecard taxonomy</h2>
				<p>We group evals by the skill you actually buy: code, reasoning, tool use, retrieval, and agent reliability.</p>
				<div class="taxonomy">
					{taxonomy.map((item) => (
						<div class="taxonomy-card">
							<strong>{item.title}</strong>
							<p>{item.description}</p>
						</div>
					))}
				</div>
			</section>

			<section class="section" id="coding">
				<h2>Coding scorecards</h2>
				<p>Bug fixes, diffs, refactors, and API patches.</p>
				{highlights.length === 0 ? (
					<p>No past scorecards yet. Check back tomorrow.</p>
				) : (
					<ul class="grid">
						{highlights.map((post) => (
							<li class="card">
								<a href={`/blog/${post.id}/`}>
									{post.data.heroImage && (
										<Image width={720} height={360} src={post.data.heroImage} alt="" />
									)}
									<h4 class="title">{post.data.title}</h4>
									<p class="date">
										<FormattedDate date={post.data.pubDate} />
									</p>
								</a>
							</li>
						))}
					</ul>
				)}
			</section>

			<section class="section" id="reasoning">
				<h2>Reasoning scorecards</h2>
				<p>Decision quality under pressure, tradeoffs, and strategy calls.</p>
				<p class="meta">More reasoning scorecards landing this week.</p>
			</section>

			<section class="section" id="tool-use">
				<h2>Tool‑use scorecards</h2>
				<p>Docs‑driven tasks, CLI accuracy, and security hygiene.</p>
				<p class="meta">More tool‑use scorecards landing this week.</p>
			</section>

			<section class="section" id="rag">
				<h2>RAG/Research scorecards</h2>
				<p>Retrieval, synthesis quality, and citation accuracy.</p>
				<p class="meta">RAG scorecards are coming soon.</p>
			</section>

			<section class="section" id="agents">
				<h2>Agent reliability scorecards</h2>
				<p>Multi‑step task success, tool failures, and recovery behavior.</p>
				<p class="meta">Agent scorecards are coming soon.</p>
			</section>

			<section class="section" id="recent">
				<h2>All recent scorecards</h2>
				{highlights.length === 0 ? (
					<p>No past scorecards yet. Check back tomorrow.</p>
				) : (
					<ul class="grid">
						{highlights.map((post) => (
							<li class="card">
								<a href={`/blog/${post.id}/`}>
									{post.data.heroImage && (
										<Image width={720} height={360} src={post.data.heroImage} alt="" />
									)}
									<h4 class="title">{post.data.title}</h4>
									<p class="date">
										<FormattedDate date={post.data.pubDate} />
									</p>
								</a>
							</li>
						))}
					</ul>
				)}
			</section>
		</main>
		<Footer />
	</body>
</html>
